<%= form_for(@customcatalog) do |f| %>
  <% if @customcatalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customcatalog.errors.count, "error") %> prohibited this customcatalog from being saved:</h2>

      <ul>
      <% @customcatalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :features %><br>
    <%= f.text_area :features %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
